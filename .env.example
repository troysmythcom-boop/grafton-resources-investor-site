# Server-only keys. Never prefix secrets with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional double-opt-in subscriber integration. Receives {email, consent:true}.
SUBSCRIBE_WEBHOOK_URL=
SUBSCRIBE_WEBHOOK_TOKEN=
PORT=5173
